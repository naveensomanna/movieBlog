import React from 'react';
import Loader from 'react-loaders'
import { packman} from 'react-loaders';
export default function RenderLoader() {
    return <Loader type="pacman" active color="black"/>
}